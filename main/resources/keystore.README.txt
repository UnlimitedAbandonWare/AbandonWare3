keystore.p12 is intentionally excluded from VCS for security.
Put your environment-specific keystore here at deploy time, or configure a path via env:
  KEYSTORE_PATH=/secure/path/keystore.p12
  KEYSTORE_PASSWORD=********
