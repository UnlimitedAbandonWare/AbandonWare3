# Feature toggles injected by src111_merge15 patch
rerank.dpp.enabled=true
rerank.dpp.k=10
onnx.max.concurrent=4
onnx.acquire.timeout.ms=150
overdrive.enabled=true
extremez.enabled=true
fusion.wpm.enabled=true
fusion.wpm.p=2.2
gate.citation.min=3
gate.final.k=6.0
gate.final.x0=0.78
gate.final.pass=0.90

llm.engine=auto

llm.route.mode=tp

onnx.enabled=${ONNX_ENABLED:false}

onnx.model.path.cross-encoder=${ONNX_MODEL_PATH:}

retrieval.vector.enabled=true

probe.search.enabled=true

probe.admin-token=${PROBE_ADMIN_TOKEN:${DOMAIN_ALLOWLIST_ADMIN_TOKEN:}}
llm.base-url=http://localhost:11434/v1
llm.api-key=${LLM_API_KEY:}
llm.chat-model=gemma3:27b
selfask.enabled=false

openai.api-key=${OPENAI_API_KEY:}
llm:
  provider: local
  base-url: http://localhost:11434/v1
  api-key: ${LLM_API_KEY:}
  chat-model: gemma3:27b
