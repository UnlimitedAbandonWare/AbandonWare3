# ---- auto-injected optimization profile ----
retrieval:
  vector:
    enabled: true

naver:
  hedge:
    enabled: true
    delay-ms: 120
  search:
    timeout-ms: 2500
    web-top-k: 10
    cache:
      enabled: true
      ttl-seconds: 3600

onnx:
  enabled: true

probe:
  search:
    enabled: false

upstash:
  cache:
    ttl-seconds: 3600

gate:
  citation:
    enabled: true
    min: 3
  finalSigmoid:
    enabled: true
    k: 12.0
    x0: 0.0

zsys:
  onnx:
    max-concurrency: 2

# Example domain whitelist
naver:
  filters:
    domain-allowlist:
      official: ["go.kr", "edu"]
